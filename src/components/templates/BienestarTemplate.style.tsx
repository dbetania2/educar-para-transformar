import { createStyles } from "@mantine/emotion";

export const useStyles = createStyles(() => ({
  infoContent: {
    minWidth: 0,
    flex: 1,
  },
  wrapAnywhere: {
    overflowWrap: "anywhere",
  },
  flexibleCopy: {
    minWidth: 0,
    flex: 1,
  },
}));
